# Interaction & Motion
- Use `.transition-standard` for hover/focus transitions.
- Durations: fast 150ms, normal 250ms, slow 400ms.
- Respect `prefers-reduced-motion` (avoid non-essential animations).
