import { Meta, Canvas } from '@storybook/blocks';
import { Button } from '../components/Button';
import { Input } from '../components/Input';
import { Card, CardHeader, CardBody } from '../components/Card';
<Meta title="Docs/Usage Rules" />
# Usage Rules
Minimal, modern, uncluttered.
<Canvas>
  <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
    <Button>Primary</Button><Button variant="secondary">Secondary</Button><Button variant="ghost">Ghost</Button><Button variant="danger">Danger</Button>
  </div>
</Canvas>
<Canvas>
  <div style={{ maxWidth: 420 }} className="space-y-6">
    <div className="space-y-2">
      <Input label="Business name" requiredMark hint="Shown to clients." />
      <Input label="Contact email" type="email" error="Enter a valid email address." />
    </div>
    <Card style={{ maxWidth: 420 }}><CardHeader title="Confirmed suppliers" subtitle="Your active bookings" /><CardBody><div className="text-2xl font-semibold">7</div></CardBody></Card>
  </div>
</Canvas>
