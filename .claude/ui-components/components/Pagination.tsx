import * as React from 'react'; import { Button } from './Button'; export function Pagination({ page, pages, onPage }: { page: number; pages: number; onPage: (p: number) => void }) { return (<div className='flex items-center justify-between gap-3'><Button variant='secondary' onClick={() => onPage(Math.max(1, page-1))} disabled={page<=1}>Previous</Button><div className='text-sm'>{page} / {pages}</div><Button variant='secondary' onClick={() => onPage(Math.min(pages, page+1))} disabled={page>=pages}>Next</Button></div>); }