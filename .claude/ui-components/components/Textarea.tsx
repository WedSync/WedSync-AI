import * as React from 'react'; export function Textarea({ label, hint, error, ...props }: React.TextareaHTMLAttributes<HTMLTextAreaElement> & { label?: string; hint?: string; error?: string }) { const id = React.useId(); return (<label className='block'>{label && <span className='text-sm font-medium'>{label}</span>}<textarea id={id} className='mt-2 w-full rounded-md border border-border bg-background p-3 shadow-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/40 focus:border-accent' {...props} />{hint && !error && <p className='mt-1 text-sm text-muted-foreground'>{hint}</p>}{error && <p className='mt-1 text-sm text-danger'>{error}</p>}</label>); }