import * as React from 'react'; export function FileUpload({ label, onFiles }: { label?: string; onFiles?: (files: FileList) => void }) { const inputRef = React.useRef<HTMLInputElement | null>(null); return (<div className='rounded-lg border border-dashed border-border p-6 text-center'>{label && <div className='text-sm font-medium'>{label}</div>}<p className='mt-1 text-sm text-muted-foreground'>Drag & drop files here or</p><button className='mt-3 inline-flex h-9 items-center rounded-md border border-border bg-background px-3 text-sm shadow-sm' onClick={() => inputRef.current?.click()} type='button'>Browse files</button><input ref={inputRef} type='file' className='hidden' multiple onChange={(e)=> e.target.files && onFiles?.(e.target.files)} /></div>); }