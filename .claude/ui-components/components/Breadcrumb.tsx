import * as React from 'react'; export function Breadcrumb({ items }: { items: { label: string; href?: string }[] }) { return (<nav aria-label='Breadcrumb' className='text-sm'><ol className='flex items-center gap-2'>{items.map((it,i)=>(<li key={i} className='flex items-center gap-2'>{i>0 && <span aria-hidden='true'>/</span>}<a href={it.href || '#'} className='text-muted-foreground hover:underline'>{it.label}</a></li>))}</ol></nav>); }