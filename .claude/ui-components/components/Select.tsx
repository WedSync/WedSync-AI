import * as React from 'react'; export function Select({ label, children, ...props }: React.SelectHTMLAttributes<HTMLSelectElement> & { label?: string }) { const id = React.useId(); return (<label className='block'>{label && <span className='text-sm font-medium'>{label}</span>}<select id={id} className='mt-2 h-10 w-full rounded-md border border-border bg-background px-3 shadow-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/40 focus:border-accent' {...props}>{children}</select></label>); }