import * as React from 'react'; import { Card, CardBody } from './Card'; export function StatCard({title,value,delta}:{title:string;value:string;delta?:string}){return(<Card><CardBody><div className='text-sm font-medium text-muted-foreground'>{title}</div><div className='mt-2 text-2xl font-semibold'>{value}</div>{delta&&<div className='mt-1 text-sm'>{delta}</div>}</CardBody></Card>);}