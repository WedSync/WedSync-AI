import * as React from 'react'; import clsx from 'clsx'; export function Avatar({ src, name, size=32 }: { src?: string; name?: string; size?: number }) { const initials = (name?.split(' ').map(n=>n[0]).slice(0,2).join('') || '•').toUpperCase(); return (<div className={clsx('flex items-center justify-center rounded-full bg-muted text-sm font-medium')} style={{ width: size, height: size }} aria-label={name}>{src ? <img src={src} alt={name} className='h-full w-full rounded-full object-cover' /> : initials}</div>); }