/**
 * WS-224: Progress Charts System - Progress Dashboard Page
 * Main page component for wedding progress visualization
 */

import { Suspense } from 'react';
import { Metadata } from 'next';
import { ProgressCharts } from '@/components/charts/ProgressCharts';

export const metadata: Metadata = {
  title: 'Progress & Analytics - WedSync',
  description: 'Track wedding planning milestones, task progress, and vendor performance with comprehensive analytics dashboards.',
};

interface ProgressPageProps {
  searchParams: {
    weddingId?: string;
    organizationId?: string;
  };
}

function ProgressSkeleton() {
  return (
    <div className="space-y-6 animate-pulse">
      <div className="grid gap-4 md:grid-cols-4">
        {[...Array(4)].map((_, i) => (
          <div key={i} className="h-24 bg-gray-200 rounded-lg"></div>
        ))}
      </div>
      <div className="h-96 bg-gray-200 rounded-lg"></div>
    </div>
  );
}

export default async function ProgressPage({ searchParams }: ProgressPageProps) {
  const { weddingId, organizationId } = searchParams;

  return (
    <div className="container mx-auto py-6 px-4 sm:px-6 lg:px-8">
      <div className="mb-6">
        <h1 className="text-3xl font-bold text-gray-900">Wedding Progress Dashboard</h1>
        <p className="text-gray-600 mt-2">
          Monitor your wedding planning progress with comprehensive analytics and insights.
        </p>
      </div>

      <Suspense fallback={<ProgressSkeleton />}>
        <ProgressCharts 
          weddingId={weddingId}
          organizationId={organizationId}
          accessLevel={{
            tier: 'professional',
            features: {
              basicProgress: true,
              advancedCharts: true,
              budgetAnalytics: true,
              vendorMetrics: true,
              customReports: true,
              realTimeUpdates: true,
              dataExport: true,
              customDashboard: true
            }
          }}
          className="space-y-6"
        />
      </Suspense>
    </div>
  );
}