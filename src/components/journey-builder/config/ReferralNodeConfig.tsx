export function ReferralNodeConfig({
  config,
  onChange,
}: {
  config: Record<string, any>;
  onChange: (config: Record<string, any>) => void;
}) {
  return (
    <div className="p-4 text-center text-muted-foreground">
      Configuration for ReferralNodeConfig coming soon...
    </div>
  );
}
